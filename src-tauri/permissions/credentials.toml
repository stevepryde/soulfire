[[permission]]
identifier = "allow-credentials"
description = "Allows the main Soulfire window to check, save, and clear the local OpenAI API key without exposing the raw stored secret."
commands.allow = [
  "get_openai_credential_status",
  "save_openai_credential",
  "delete_openai_credential",
]
